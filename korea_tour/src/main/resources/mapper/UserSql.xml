<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bitcamp.korea_tour.model.mapper.UserMapper">
	<select id="getUserList" resultType="user">
		select * from user order by userNum desc
	</select>
	<select id="getUserData" parameterType="String" resultType="user">
		select * from user where userNum=#{userNum}
	</select>
	<insert id="insertUser" parameterType="user">
		INSERT INTO user (email, name, birth, gender, photo, kakaoKey, naverKey, googleKey) 
		VALUES
		(#{email}, #{name}, #{birth}, #{gender}, #{photo}, #{kakaoKey}, #{naverKey}, #{googleKey}
		)
	</insert>
	<delete id="deleteUser" parameterType="String">
		delete from user where userNum=#{userNum}
	</delete>
	<select id="hasKey" parameterType="String" resultType="int">
		select count(*) from user where kakaoKey=#{key}
	</select>

</mapper>