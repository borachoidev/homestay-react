<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bitcamp.korea_tour.model.mapper.HomeStayMarkMapper">
	<!-- <select id="getMarkList" parameterType="int" resultType="joinmark">
		select m.homeStayMarkNum, m.userNum as customNum, m.homeStayNum, h.userNum as hostNum, h.title, h.addr1, h.content, h.approval, h.price, h.open  
		from homeStayMark m left join homeStay h on m.homeStayNum=h.homeStayNum where m.userNum=#{userNum} order by homeStayMarkNum desc
	</select>
	<select id="getTotalCount" parameterMap="int" resultType="int">
		select count(*) from homeStayMark where userNum=#{userNum}
	</select> -->
</mapper>